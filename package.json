{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@11.1.2",
  "scripts": {
    "preinstall": "node -e \"const fs=require('node:fs'); for (const file of ['package-lock.json','yarn.lock']) fs.rmSync(file,{force:true}); const ua=process.env.npm_config_user_agent||''; const execPath=(process.env.npm_execpath||'').toLowerCase(); if (!ua.startsWith('pnpm/') && !execPath.includes('pnpm')) { console.error('Use pnpm instead'); process.exit(1); }\"",
    "prepare": "node -e \"const {execSync}=require('node:child_process'); try { execSync('git rev-parse --git-dir',{stdio:'ignore'}); execSync('git config core.hooksPath .husky',{stdio:'ignore'}); } catch {}\"",
    "secret-scan": "pnpm --silent --filter @workspace/scripts exec tsx ../scripts/src/secret-scan.ts --all",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
